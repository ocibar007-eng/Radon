rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras permissivas para desenvolvimento
    // IMPORTANTE: Alterar antes de ir para produção!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
